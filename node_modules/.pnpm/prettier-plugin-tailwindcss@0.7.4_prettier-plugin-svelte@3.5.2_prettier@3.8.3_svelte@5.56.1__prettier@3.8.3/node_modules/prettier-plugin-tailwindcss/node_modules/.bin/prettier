#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../prettier/bin/prettier.cjs" "$@"
else
  exec node  "$basedir/../../../prettier/bin/prettier.cjs" "$@"
fi
# cmd-shim-target=/home/cioky/Projects/app/node_modules/.pnpm/prettier-plugin-tailwindcss@0.7.4_prettier-plugin-svelte@3.5.2_prettier@3.8.3_svelte@5.56.1__prettier@3.8.3/node_modules/prettier/bin/prettier.cjs
