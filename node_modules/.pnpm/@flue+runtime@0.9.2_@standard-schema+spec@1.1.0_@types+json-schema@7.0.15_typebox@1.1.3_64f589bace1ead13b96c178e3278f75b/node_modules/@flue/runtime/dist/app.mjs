//#region src/app.ts
function migrationError(helper, entrypoint) {
	throw new Error(`[flue] ${helper}() is no longer available from "@flue/runtime/app". Import it from "${entrypoint}" instead.`);
}
const admin = () => migrationError("admin", "@flue/runtime/routing");
const flue = () => migrationError("flue", "@flue/runtime/routing");
const configureProvider = () => migrationError("configureProvider", "@flue/runtime");
const registerApiProvider = () => migrationError("registerApiProvider", "@flue/runtime");
const registerProvider = () => migrationError("registerProvider", "@flue/runtime");
const observe = () => {
	throw new Error("[flue] observe() is no longer available from \"@flue/runtime/app\". Import it from \"@flue/runtime\" instead.");
};
//#endregion
export { admin, configureProvider, flue, observe, registerApiProvider, registerProvider };
