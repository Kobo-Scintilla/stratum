import { C as FlueEvent, E as FlueHarness, S as FlueContext, b as DispatchReceipt, et as SessionEnv, f as AttachedAgentEventCallback, i as AgentHarnessOptions, t as AgentConfig, tt as SessionStore, v as CreatedAgent, w as FlueEventCallback, y as DirectAgentPayload } from "./types-CEfuEx4p.mjs";

//#region src/runtime/dispatch-queue.d.ts
interface DispatchInput {
  dispatchId: string;
  agent: string;
  id: string;
  session: string;
  input: unknown;
  acceptedAt: string;
}
declare function assertCurrentDispatchInput(value: unknown): asserts value is DispatchInput;
interface DispatchProcessor {
  process(input: DispatchInput): Promise<void> | void;
}
interface DispatchQueue {
  enqueue(input: DispatchInput): Promise<DispatchReceipt>;
}
/**
 * Process-lifetime dispatch queue. Acceptance currently means the dispatch was
 * validated, appended to this in-memory queue, and scheduled for the configured
 * processor. Queued items are lost if the process/isolate exits before the
 * placeholder processor runs. Durable backends should implement DispatchQueue
 * without changing dispatch() authoring semantics.
 */
declare class InMemoryDispatchQueue implements DispatchQueue {
  private readonly processor;
  private readonly pending;
  private scheduled;
  constructor(processor?: DispatchProcessor);
  enqueue(input: DispatchInput): Promise<DispatchReceipt>;
  private scheduleDrain;
  private drain;
}
//#endregion
//#region src/client.d.ts
interface FlueContextConfig {
  id: string;
  runId?: string;
  dispatchId?: string;
  payload: any;
  env: Record<string, any>;
  agentConfig: AgentConfig;
  createDefaultEnv: () => Promise<SessionEnv>;
  defaultStore: SessionStore;
  /**
   * Platform-specific sandbox resolver hook. Called before default resolution.
   * Returns SessionEnv to use, or null to fall through to default logic.
   */
  resolveSandbox?: (sandbox: unknown) => Promise<SessionEnv> | null;
  /**
   * The current HTTP request, if any. Surfaced to handlers as `ctx.req`.
   * Build plugins pass the standard Fetch `Request` through; non-HTTP entry
   * points (e.g. future cron triggers) leave it undefined.
   */
  req?: Request;
  initialEventIndex?: number;
}
/** Extends FlueContext with server-only methods. Agent handlers only see FlueContext. */
interface FlueContextInternal extends FlueContext {
  readonly runId: string | undefined;
  initializeCreatedAgent(agent: CreatedAgent, payload: unknown, options?: AgentHarnessOptions): Promise<FlueHarness>;
  emitEvent(event: FlueEvent): FlueEvent;
  subscribeEvent(callback: FlueEventCallback): () => void;
  setEventCallback(callback: FlueEventCallback | undefined): void;
}
declare function createFlueContext(config: FlueContextConfig): FlueContextInternal;
//#endregion
//#region src/runtime/run-store.d.ts
type RunStatus = 'active' | 'completed' | 'errored';
interface RunRecord {
  runId: string;
  owner: RunOwner;
  status: RunStatus;
  startedAt: string;
  payload?: unknown;
  endedAt?: string;
  isError?: boolean;
  durationMs?: number;
  result?: unknown;
  error?: unknown;
}
interface CreateRunInput {
  runId: string;
  owner: RunOwner;
  startedAt: string;
  payload: unknown;
}
interface EndRunInput {
  runId: string;
  endedAt: string;
  isError: boolean;
  durationMs: number;
  result?: unknown;
  error?: unknown;
}
interface RunStore {
  createRun(input: CreateRunInput): Promise<void>;
  endRun(input: EndRunInput): Promise<void>;
  appendEvent(runId: string, event: FlueEvent): Promise<void>;
  getEvents(runId: string, fromIndex?: number): Promise<FlueEvent[]>;
  getRun(runId: string): Promise<RunRecord | null>;
}
//#endregion
//#region src/runtime/run-registry.d.ts
type RunOwner = {
  kind: 'workflow';
  workflowName: string;
  instanceId: string;
};
interface RecordRunStartInput {
  runId: string;
  owner: RunOwner;
  startedAt: string;
}
interface RunPointer {
  runId: string;
  owner: RunOwner;
  status: RunStatus;
  startedAt: string;
  endedAt?: string;
  durationMs?: number;
  isError?: boolean;
}
interface RecordRunEndInput {
  runId: string;
  endedAt: string;
  durationMs: number;
  isError: boolean;
}
interface ListRunsOpts {
  status?: RunStatus;
  workflowName?: string;
  limit?: number;
  cursor?: string;
}
interface ListRunsResponse {
  runs: RunPointer[];
  nextCursor?: string;
}
interface RunRegistry {
  recordRunStart(input: RecordRunStartInput): Promise<void>;
  recordRunEnd(input: RecordRunEndInput): Promise<void>;
  lookupRun(runId: string): Promise<RunPointer | null>;
  listRuns(opts?: ListRunsOpts): Promise<ListRunsResponse>;
}
//#endregion
//#region src/runtime/run-subscribers.d.ts
type RunSubscriberListener = (event: FlueEvent) => void;
interface RunSubscriberRegistry {
  subscribe(runId: string, listener: RunSubscriberListener): () => void;
  publish(runId: string, event: FlueEvent): void;
  /** Release registry state for a terminal run. */
  complete(runId: string): void;
}
declare function createRunSubscriberRegistry(): RunSubscriberRegistry;
//#endregion
//#region src/runtime/handle-agent.d.ts
/** Direct agent handler signature used by attached HTTP and WebSocket prompts. */
type AgentHandler = (ctx: FlueContextInternal) => unknown | Promise<unknown>;
type CreatedAgentHandler = CreatedAgent;
type WorkflowHandler = (ctx: FlueContextInternal) => unknown | Promise<unknown>;
interface AgentSessionTarget {
  agentName: string;
  instanceId: string;
}
declare function createAgentDispatchProcessor(options: {
  agents: Record<string, CreatedAgentHandler>;
  createContext: CreateContextFn;
}): DispatchProcessor;
interface ValidateAgentDispatchAdmissionOptions {
  input: DispatchInput;
}
declare function validateAgentDispatchAdmission(options: ValidateAgentDispatchAdmissionOptions): Promise<DispatchReceipt>;
declare function createDispatchAgentHandler(agent: CreatedAgentHandler, input: DispatchInput): AgentHandler;
declare function reserveDispatchAgentSession(target: AgentSessionTarget, payload: unknown): Promise<() => void>;
declare function createDirectAgentHandler(agent: CreatedAgentHandler): AgentHandler;
/**
 * Caller-provided context factory. Differs per-target:
 *   - Node: env=process.env, defaultStore=in-memory, no resolveSandbox.
 *   - Cloudflare: env=DO env, defaultStore=DO SQLite, resolveSandbox=cfSandboxToSessionEnv.
 */
type CreateContextFn = (id: string, runId: string | undefined, payload: unknown, request: Request, initialEventIndex?: number, dispatchId?: string) => FlueContextInternal;
/**
 * Background workflow admission wrapper. Receives the prepared workflow-run
 * callback and returns a promise that resolves with its result. Implementations:
 *
 *   - Node: just `run()` — no fiber, no DO.
 *   - Cloudflare: `doInstance.runFiber('flue:workflow:<runId>', run)`.
 *
 * The caller is responsible for any logging on completion/error; this wrapper
 * starts durably admitted workflow execution for any supported observation mode.
 */
type StartWorkflowAdmissionFn = (runId: string, run: () => Promise<unknown>) => Promise<unknown>;
/**
 * Direct-agent foreground execution wrapper. Wraps the call to `handler(ctx)`
 * so targets can layer in keepalive / context propagation. Defaults to direct
 * invocation when omitted.
 */
type RunHandlerFn = (ctx: FlueContextInternal, handler: AgentHandler) => unknown | Promise<unknown>;
interface HandleAgentOptions {
  request: Request;
  agentName: string;
  id: string;
  handler: AgentHandler;
  createContext: CreateContextFn;
  runHandler?: RunHandlerFn;
}
interface HandleWorkflowOptions {
  request: Request;
  workflowName: string;
  handler: WorkflowHandler;
  createContext: CreateContextFn;
  startWorkflowAdmission?: StartWorkflowAdmissionFn;
  runStore?: RunStore;
  runSubscribers?: RunSubscriberRegistry;
  runRegistry?: RunRegistry;
  runId?: string;
}
/**
 * Handle one attached `/agents/:name/:id` prompt interaction.
 *
 * `Accept: text/event-stream` returns attached event streaming; otherwise the
 * response is synchronous JSON `{ result }`. Former `X-Webhook: true` agent
 * requests are rejected because asynchronous delivery uses `dispatch(...)`.
 *
 * Errors thrown before streaming starts are returned as regular HTTP error
 * responses; errors thrown after SSE begins are framed as stream errors.
 */
declare function handleAgentRequest(opts: HandleAgentOptions): Promise<Response>;
declare function handleWorkflowRequest(opts: HandleWorkflowOptions): Promise<Response>;
interface InvokeWorkflowAttachedOptions {
  owner: RunOwner;
  id: string;
  runId: string;
  handler: WorkflowHandler;
  payload: unknown;
  request: Request;
  createContext: CreateContextFn;
  startWorkflowAdmission?: StartWorkflowAdmissionFn;
  onAdmitted?: (runId: string) => void;
  onEvent?: FlueEventCallback;
  emitIdleOnComplete?: boolean;
  runStore?: RunStore;
  runSubscribers?: RunSubscriberRegistry;
  runRegistry?: RunRegistry;
}
interface DirectAttachedOptions {
  agentName: string;
  id: string;
  handler: AgentHandler;
  payload: DirectAgentPayload;
  request: Request;
  createContext: CreateContextFn;
  runHandler?: RunHandlerFn;
  onEvent?: AttachedAgentEventCallback;
  emitIdleOnComplete?: boolean;
}
interface WorkflowAttachedInvocationResult {
  runId: string;
  result: unknown;
}
interface FailRecoveredRunOptions {
  owner: RunOwner;
  id: string;
  runId: string;
  request: Request;
  createContext: CreateContextFn;
  error: unknown;
  runStore?: RunStore;
  runSubscribers?: RunSubscriberRegistry;
  runRegistry?: RunRegistry;
}
declare function failRecoveredRun(opts: FailRecoveredRunOptions): Promise<void>;
declare function invokeDirectAttached(opts: DirectAttachedOptions): Promise<unknown>;
declare function invokeWorkflowAttached(opts: InvokeWorkflowAttachedOptions): Promise<WorkflowAttachedInvocationResult>;
//#endregion
export { RecordRunStartInput as A, FlueContextInternal as B, validateAgentDispatchAdmission as C, ListRunsOpts as D, createRunSubscriberRegistry as E, EndRunInput as F, InMemoryDispatchQueue as G, DispatchInput as H, RunRecord as I, assertCurrentDispatchInput as K, RunStatus as L, RunPointer as M, RunRegistry as N, ListRunsResponse as O, CreateRunInput as P, RunStore as R, reserveDispatchAgentSession as S, RunSubscriberRegistry as T, DispatchProcessor as U, createFlueContext as V, DispatchQueue as W, failRecoveredRun as _, DirectAttachedOptions as a, invokeDirectAttached as b, HandleWorkflowOptions as c, StartWorkflowAdmissionFn as d, WorkflowAttachedInvocationResult as f, createDispatchAgentHandler as g, createDirectAgentHandler as h, CreatedAgentHandler as i, RunOwner as j, RecordRunEndInput as k, InvokeWorkflowAttachedOptions as l, createAgentDispatchProcessor as m, AgentSessionTarget as n, FailRecoveredRunOptions as o, WorkflowHandler as p, CreateContextFn as r, HandleAgentOptions as s, AgentHandler as t, RunHandlerFn as u, handleAgentRequest as v, RunSubscriberListener as w, invokeWorkflowAttached as x, handleWorkflowRequest as y, FlueContextConfig as z };