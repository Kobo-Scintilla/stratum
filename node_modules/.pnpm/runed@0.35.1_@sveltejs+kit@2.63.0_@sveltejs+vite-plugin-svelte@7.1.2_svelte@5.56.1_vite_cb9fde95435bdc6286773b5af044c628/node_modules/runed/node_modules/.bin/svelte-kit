#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@sveltejs/kit/svelte-kit.js" "$@"
else
  exec node  "$basedir/../../../@sveltejs/kit/svelte-kit.js" "$@"
fi
# cmd-shim-target=/home/cioky/Projects/app/node_modules/.pnpm/runed@0.35.1_@sveltejs+kit@2.63.0_@sveltejs+vite-plugin-svelte@7.1.2_svelte@5.56.1_vite_cb9fde95435bdc6286773b5af044c628/node_modules/@sveltejs/kit/svelte-kit.js
