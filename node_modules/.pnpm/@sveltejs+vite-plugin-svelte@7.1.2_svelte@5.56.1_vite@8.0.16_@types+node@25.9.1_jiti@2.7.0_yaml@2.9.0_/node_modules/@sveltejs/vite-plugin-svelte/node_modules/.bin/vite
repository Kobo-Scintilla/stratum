#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../vite/bin/vite.js" "$@"
else
  exec node  "$basedir/../../../../vite/bin/vite.js" "$@"
fi
# cmd-shim-target=/home/cioky/Projects/app/node_modules/.pnpm/@sveltejs+vite-plugin-svelte@7.1.2_svelte@5.56.1_vite@8.0.16_@types+node@25.9.1_jiti@2.7.0_yaml@2.9.0_/node_modules/vite/bin/vite.js
